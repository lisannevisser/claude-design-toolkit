# Content Guidelines — `<projektname>`

> ⚠️ **TEMPLATE — dünner Zeiger.** Primäres Dokument des `content-advisor`. **Wenn es bereits eine
> Content-/Copy-Guideline gibt (Repo/MD/Confluence), hier nur VERLINKEN — nicht duplizieren.**
> Nur falls keine existiert, die Punkte unten ausfüllen.

## Quelle der Wahrheit
- **Content-Guidelines:** `<link/pfad>` (Voice & Tone, Terminologie, Schreibregeln).
- **Echte Texte:** `<produkt-repo>/<translations-pfad>` — Quelle für „real content only".

## Projekt-Deltas (nur falls keine externe Quelle existiert)
- **Voice & Tone:** `<z. B. klar, vertrauenswürdig, nicht werblich am Zahlpunkt>`
- **Terminologie:** `<verbindliche Begriffe, z. B. „Mitgliedschaft" statt „Abo">`
- **Buttons/Labels:** `<Konventionen für Primäraktionen, Verben>`
- **Fehlermeldungen:** `<verzeihend, konkret, lösungsorientiert; kein Tech-Jargon>`
- **i18n:** Locales `<…>` — manche laufen länger (Layouts müssen tolerieren); kein hardcoded Text.

## Akzeptanz (Content)
- Real content, **kein Lorem Ipsum**.
- Konsistente Terminologie & Tonalität gemäß Quelle.
- Fehler-/Empty-/Loading-Texte vorhanden und hilfreich.
- Lokalisiert; Layout toleriert lange Sprachen.
